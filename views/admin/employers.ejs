<% include ../shared/header.ejs %>
<%if (user.userType == "admin"){%>
<div class="container">
<h1 style="text-align: center; font-weight: bold; padding: 20px">All Employers</h1>
<table class="table table-striped table-bordered table-hover">
    <caption>All Employers List</caption>
    <tr>
        <th>Username</th>
        <th>Company Name</th>
        <th>Contact Person</th>
    </tr>
    <% for(i=0; i<employers.length; i++) { %>
    <tr>
        <td><%= employers[i].username %></td>
        <td><%= employers[i].cName %></td>
        <td><%= employers[i].cPerson %></td>
    </tr>
    <%}%>
</table>
<%}
else{%>
<h1> Sorry You do not have Permission to access this page.</h1>
<%}%>
</div>
<% include ../shared/footer.ejs %>